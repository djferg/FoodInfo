<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoodInfo.Views.ProductView"
             Style="{DynamicResource PageStyle}"
             xmlns:vm="clr-namespace:FoodInfo.Models"
             Title="Product Details">
    <StackLayout>
        <Label Text="Code" />
        <Label Text="{Binding Barcode}" />
        <Label Text="Name" />
        <Label Text="{Binding Name}" />
        <Image Source="{Binding ImageUrl}" WidthRequest="100" />

        <Label Text="Ingredients" FontAttributes="Bold" />

        <ListView ItemsSource="{Binding Ingredients}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Text}" Detail="{Binding Id}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>